interface:
  display_name: "GPT Image"
  short_description: "Complete GPT Image 2 skill for generations, edits, and responses."
  default_prompt: "Use $gpt-image to generate, edit, or stream GPT Image 2 outputs through an OpenAI-compatible API."
