# Device-local shell files — never synced across machines
.zprofile
.zshrc.local
.zshenv.local

# Device-local git config (per-machine signing key path)
.gitconfig.local

# Machine-local Claude rules (client/tenant mappings)
.claude/rules/acli-tenants.md
